#include "Global_Variables.h"
#include "v5CArrayData.h"

#ifndef __COMMON_FUNCTIONS_H__
#define __COMMON_FUNCTIONS_H__

namespace BrokenTechStudios
{
	namespace Nuke
	{
		namespace Core
		{/*
			static v5CArrayData<WORD> __GROUP_WORD_ADD__(int offset, WORD* cArray, unsigned int size)
			{
				WORD* temp = (WORD*)malloc(size);
				for(int i = 0; i < size; i++)
				{
					temp[i] = cArray[i] + offset;
				};
				return v5CArrayData<WORD>(temp, size);
			};*/
		};
	};
};

#endif